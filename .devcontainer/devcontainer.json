{
    "name": "data_platform",
    "dockerComposeFile": [
        "../compose.yaml",
        "compose.yaml"
    ],
    "service": "app",
    "runServices": [
        "app",
        "rustfs"
    ],
    "workspaceFolder": "/workspace",
    "features": {
        "ghcr.io/dhoeric/features/hadolint:1": {}
    },
    "customizations": {
        "vscode": {
            "extensions": [
                "github.copilot",
                "github.copilot-chat",
                "charliermarsh.ruff",
                "exiasr.hadolint",
                "kevinrose.vsc-python-indent",
                "mosapride.zenkaku",
                "ms-azuretools.vscode-docker",
                "ms-python.python",
                "ms-python.vscode-pylance",
                "njpwerner.autodocstring",
                "redhat.vscode-yaml",
                "shardulm94.trailing-spaces",
                "tamasfe.even-better-toml",
                "yzhang.markdown-all-in-one"
            ]
        }
    },
    "containerEnv": {
        "DISPLAY": "dummy",
        "UV_PROJECT_ENVIRONMENT": "/home/vscode/.venv",
        "UV_GROUPS": "dev",
        "GOOGLE_APPLICATION_CREDENTIALS": "/workspace/.secrets/keita-masui-firstproject-46adeb7731b9.json",
        "GEMINI_API_KEY": "${localEnv:GEMINI_API_KEY}",
        "ANTHROPIC_API_KEY": "${localEnv:ANTHROPIC_API_KEY}",
        "GH_TOKEN": "${localEnv:GH_TOKEN}",
        "EDITOR": "code --wait"
    },
    "postCreateCommand": "uv sync --locked $(for g in $UV_GROUPS; do printf ' --group %s' \"$g\"; done) && git config --global user.name 'Keita Masui' && git config --global user.email 'rfrnndam.tennis365@gmail.com' && (gh copilot -- --help >/dev/null 2>&1 || true)",
    "postStartCommand": "uv run pre-commit install || true",
    "remoteUser": "vscode",
    "mounts": [
        "source=${localEnv:HOME}/.claude,target=/home/vscode/.claude,type=bind,consistency=cached",
        "source=${localEnv:HOME}/.config/gh,target=/home/vscode/.config/gh,type=bind,consistency=cached"
    ]
}
